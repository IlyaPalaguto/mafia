<div class="form-card">
  <%# TODO: пока так, но нужно исправить %>
  <% players = doctor.game_session.players.excluding doctor.check_ability ? nil : doctor.player %>

  <%= form.collection_radio_buttons :save_target_id, players, :id, :name do |radio_button| %>
    <div class="input-container">
      <%= radio_button.radio_button class: "checkbox-hidden" %>
      <div class="input-checkbox">
        <%= radio_button.label class: "player-name" %>
      </div>
    </div>
  <% end %>
</div>

<%= button_tag "Make Action", type: "submit", class: "button-secondary" %>
